# This is a sample .env file for use in local development.
# Duplicate this file as .env in the api-service root directory and update the environment variables to match your desired config

PORT=''
NODE_ENV=''